# Local development — leave empty to use Vite's proxy (recommended)
VITE_API_URL=

# Production — set this to your Render backend URL in Netlify env vars
# VITE_API_URL=https://journal-backend.onrender.com
